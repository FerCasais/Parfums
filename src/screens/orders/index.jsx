import { View, Text } from "react-native";
import { styles } from "./style";

const Orders = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.orderListText}>this is the orders list in progress</Text>
    </View>
  );
};

export default Orders;
